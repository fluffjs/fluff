import { describe, expect, it } from 'vitest';
import { FluffRouter } from './FluffRouter.js';

describe('FluffRouter', () =>
{
    it('should be defined', () =>
    {
        expect(FluffRouter)
            .toBeDefined();
    });
});
