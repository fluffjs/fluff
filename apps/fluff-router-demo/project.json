{
    "name": "fluff-router-demo",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "sourceRoot": "apps/fluff-router-demo/src",
    "tags": [],
    "targets": {
        "build": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm run cli -- --cwd apps/fluff-router-demo build"
            },
            "outputs": [
                "{projectRoot}/dist"
            ]
        },
        "serve": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm run cli -- --cwd apps/fluff-router-demo serve"
            }
        }
    }
}
